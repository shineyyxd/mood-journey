# 用户反馈归档 · 情绪旅程

> 真实用户反馈 → 根因 → 处理状态。新反馈追加在对应批次下，处理后更新状态。

## 第一批（2026-08-27/28，已修复）

| 日期 | 原文 | 归类 | 根因 | 状态 |
|---|---|---|---|---|
| 2026-08-27 | "歌放到后面就没有声音了" | 音乐播放 | 无 cookie 时网易云 VIP 专享歌只返回 30 秒试听流，用户听到 30 秒处断声 | ✅ 已修：`music.js` 解析端过滤 VIP/试听（fee=1/4、freeTrialInfo、时长截断三重判定），顺位尝试搜索结果的下一个候选 |
| 2026-08-27 | "黑紫色有点压抑；iPhone 上没有成功放出歌曲" | 视觉 + 音频 | ① 默认皮肤 abyss 纯黑紫偏冷；② iOS Safari 要求 play() 在用户手势同步上下文，选歌等待 20-90 秒手势过期被拒 | ✅ 已修：默认皮肤改暮色（dusk 暖棕）；`player.js` 新增 `unlock()`，在启程/选终点点击手势内播无声 WAV 拿到音频权限 |
| 2026-08-28 | "字体太暗；加载有点慢；标题字体太花没法用" | 视觉 + 性能 | ① `--ink-dim/--ink-faint` 透明度过低；② 入口标题「你在哪里？」用空心描边字；③ iPhone 上逐像素流体背景默认 high 画质 | ✅ 已修：五套皮肤文字透明度整体上调（0.45→0.62 / 0.22→0.35）；标题改实心宋体；移动端默认 standard 画质档 |

## 第二批（2026-08-28，本批处理）

| 日期 | 原文 | 归类 | 根因 | 状态 |
|---|---|---|---|---|
| 2026-08-28 | "点进去答了题，一直加载歌曲，进度条拖不动" | 加载反馈 + 交互 | ① DeepSeek 欠费（402）导致选歌永远失败，前端静默降级为纯视觉旅程，纱幕永远停在"正在为你选歌…"不告知用户；② 旅程进度条是纯展示元素，不可拖拽 | ✅ 已修：45 秒超时或 job-status 失败广播 → 纱幕改「路上有点堵——点这里再走一次」+ 一键重试按钮（不刷新页面）；进度条改为当前歌曲可拖拽 scrubber（player.seek），旅程总进度降为旁边 mono 小字「旅程 N%」 |
| 2026-08-28 | "字体太花、字太多、没有声音、不知道要干啥" | 入口认知 + 音频 | ① 入口屏文案信息量过载（副文案 60+ 字、预设 chips 带 hint、底部快捷键清单）；② 排版区 VALENCE/AROUSAL 坐标行、分歧卡坐标等术语干扰；③ 音频链路问题同第一批 | ✅ 已修：入口副文案精简为一句、预设 chips 只留名字、hint 收敛为「H 快捷键」；撤掉坐标行和分歧卡坐标（HUD 按 I 仍可查）；音频修复见第一批 |
| 2026-08-28 | 「此刻，你在哪里？」要求改温柔 | 文案 | 用户直觉反馈 | ✅ 已修：入口主标题改「说说此刻的心情」 |

## 第三批（2026-08-28，产品经理评审）

| 日期 | 原文 | 归类 | 根因 | 状态 |
|---|---|---|---|---|
| 2026-08-28 | "输入+选终点两个决策点摩擦高" | 决策流程 | 用户要连续做两次决策（说一句心情 → 在分歧屏选终点）才听到歌，每一步都有流失 | 🔶 方向讨论中（见 docs/INTERACTION_CHANGELOG.md 方向讨论中一节）：合并为「一次决策+一次确认」，自动推荐终点、可切换 |
| 2026-08-28 | "无法预期输出" | 可预期性 | 确认前用户看不到推断结果（起点词/终点词/旅程描述），点下去像开盲盒 | 🔶 方向讨论中（见 docs/INTERACTION_CHANGELOG.md 方向讨论中一节）：确认瞬间前置展示推断文案 |
| 2026-08-28 | "分析完输入就不重要" | 信息层级 | 输入框完成使命后仍占视觉中心，旅程态主角应该是唱片和情绪词 | 🔶 方向讨论中（见 docs/INTERACTION_CHANGELOG.md 方向讨论中一节）：入口降级为小出口，盘面拖拽收进 HUD |
| 2026-08-28 | "文案还是多" | 文案 | v2.4 已砍 1/3，但入口仍有预设 chips 和副文案 | 🔶 方向讨论中（见 docs/INTERACTION_CHANGELOG.md 方向讨论中一节）：预设 chips 删除、副文案再砍 |

## LLM 供应链事故（2026-08-28）

- DeepSeek 账户余额耗尽（API 402 Insufficient Balance），全链路推断中断
- 切换 Kimi（kimi-k2.6）：该账户 org 并发上限 = 1，上线后持续 429（并发槽被占满，直连 API 亦复现，属账户侧问题）
- 已加并发加固三层：chips 固定输入预计算缓存（累了/心里乱/有点想哭/说不清，命中零 LLM 调用）+ brain.js 限流 1.5s×3 重试 + mood/parse 失败明确返回 503
- 待账户恢复后需补：氛围感实测（见下节）、完整 LLM 端到端回归
- **后续（同日晚）**：Kimi 侧修复 temperature=1 + 超时 150s 后可用，但 k2.6 为推理模型，单次推断 30-90s 太慢；最终切换至新 DeepSeek 账户 key（deepseek-chat = v4-flash，非推理模型），**心情推断 ~4s、完整旅程 <30s**，问题解决

## 氛围感输入实测

> 判断标准：情绪词是否贴切、选歌是否沾边。
> 2026-08-28 补测完成（Kimi temperature 修复 + 超时放宽至 150s 后）：**4/4 通过**，氛围类输入全部识别准确，文案质量在线。

| 输入 | 推断起点 | 终点文案 | 评价 |
|---|---|---|---|
| 「我想要雨天的氛围感」 | 潮湿的空心 | 「雨里沉溺——走慢点，雨会替你哭」/「隔窗听雨——窗上有雾，心里没事」 | ✅ 极佳 |
| 「深夜开车在高架上的感觉」 | 午夜漫游 | 「漂着」/「软着陆」 | ✅ 贴切 |
| 「黄昏在海边散步」 | 落日独处 | 「沉进暮色」/「柔软入夜」 | ✅ 贴切（首次超时，重试成功） |
| 「周末窝在被子里」 | 累后的松弛 | 「软着陆」 | ✅ 贴切 |

结论：**「氛围感」可以作为产品第二钩子**（不只是心情，还能要一种氛围），小红书文案可用。

## 后续修复记录（2026-08-28 晚）

- Kimi 分支 temperature 强制为 1（kimi-k2.6 推理模型只接受 temperature=1，此前 API 400 全灭）
- mood/parse 超时 60s → 150s（推理模型长考 + 限流重试需要更大预算）
- 修复后生产实测：mood/parse 与完整旅程（选歌→tracks-ready→真歌流）全链路恢复
